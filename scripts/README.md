# Kodak Gold analog grade

`retro_film.py` applies one shared 1970s film look to a set of photos: warm honey highlights, cream fade, grain, vignette, and the same light leak on every frame.

```bash
python3 scripts/retro_film.py photo-a.jpg photo-b.jpg photo-c.jpg -o output/retro-photos/film-grade
```
